{
  "manifest_version": 3,
  "name": "Color Picker Extension",
  "description": "Allows user to pick a color from any webpage and stores the selected color in chrome storage",
  "version": "1.0",
  "permissions": [
    "activeTab",
    "storage"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  }
}
